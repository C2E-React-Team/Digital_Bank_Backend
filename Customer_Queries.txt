use test
drop table if exists customers; 
create table customers(sno int(10),customer_id varchar(20),sex  varchar(10),marital_status  varchar(10),dependents varchar(10),education  varchar(20),self_employed  varchar(10),income  numeric(20,2),coapplicant_income numeric(20,2),loanamount numeric(20,2),loan_amount_term int(10),credit_history int(5),property_area  varchar(20),loan_status  varchar(10));
LOAD DATA INFILE '/Program Files/MariaDB 10.5/data/sample/Customer_Dataset.csv' INTO TABLE customers FIELDS TERMINATED BY ',' LINES TERMINATED BY '\n' IGNORE 1 LINES (sno,customer_id,sex,marital_status,dependents,education,self_employed,income,coapplicant_income,loanamount,loan_amount_term,credit_history,property_area,loan_status);

/* Loan Details Table */
drop table if exists loans;
/* CREATE TABLE */
CREATE TABLE loans(
ref_id varchar(1000) primary key,
car_name VARCHAR(100),
customer_id varchar(100),
emi DOUBLE,
car_cost DOUBLE,
selected_file VARCHAR(100),
tenure int(10),
loan_amount DOUBLE,
status varchar(100)
);

alter table loans add(selected_file_data mediumblob);